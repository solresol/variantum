[project]
name = "variantum"
version = "0.1.0"
description = "Parallage prompt packs and reviewer tools"
requires-python = ">=3.12"
dependencies = [
    "psycopg2-binary>=2.9.9",
]

[tool.uv]
package = false
